defaults:
  - dataset: brain_drr
  - model: ddpm
  - _self_

hydra:
  job:
    chdir: false          # keep cwd stable across accelerate processes
  output_subdir: null
  run:
    dir: outputs/ddpm_brain
